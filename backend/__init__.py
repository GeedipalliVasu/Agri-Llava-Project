"""
Backend package initializer.
Creating this file ensures the `backend` directory is treated as a Python package
so package-qualified imports like `backend.timeline_routes` work under gunicorn.
"""
